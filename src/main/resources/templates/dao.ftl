package ${packageName?lower_case}.mapper;

import ${packageName?lower_case}.entity.${camelName?cap_first};

/**
 * 描述：类 {@code ${camelName?cap_first}} ${cmt}Mapper
 *
 * @author ${author}
 * @date ${date?string('yyyy-MM-dd')}
 */
public interface ${camelName?cap_first}Mapper{

}